import * as config from "./dist/firebaseConfig.dev.js";

export const app = config.app;
export const auth = config.auth;
export const database = config.database;
export const storage = config.storage;
